/** Cliente piloto do Zatti Hub. Hardcoded enquanto o app é mono-tenant -
 * quando virar multi-cliente de verdade, isso vem de uma tabela de tenants
 * (mesma observação já vale pra `getSpreadsheetId` em `lib/sheets/client.ts`). */
export const NOME_CLIENTE = "Dom Quixote";
